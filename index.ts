// @amlhubs/smof — public entry point (skeleton)
//
// The namespace facade and named re-exports for every SMOF concept will be
// authored after the metaclasses land in `./smof.ts`. This file ships empty
// for the 0.0.0 scaffold; the 0.0.1 release will populate the surface.

export * from './smof.js'
